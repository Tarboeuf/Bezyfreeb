<ContentDialog
    x:Class="BezyFB_UWP.DownloadDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BezyFB_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DOWNLOAD">
    <ContentDialog.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="120" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </ContentDialog.Resources>


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Content="Télecharger" Name="buttonTelecharger" Click="Telecharger_Click" />
        <Button Content="Vu" Grid.Column="1" Click="Vu_Click"/>
        <Button Content="Sous titre" Grid.Row="1" Click="SousTitre_Click"/>
        <Button Content="Fermer" Grid.Row="1" Grid.Column="1"  Click="Annuler_Click"/>
    </Grid>
</ContentDialog>
